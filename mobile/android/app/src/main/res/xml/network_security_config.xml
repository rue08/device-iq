<?xml version="1.0" encoding="utf-8"?>
<!--
  Dev-only: allows plaintext HTTP to the local backend during development
  (10.0.2.2 is the Android emulator's alias for the host machine's
  localhost; 127.0.0.1/10.0.2.2 are used with `adb reverse` on a real
  device too). Remove this once the backend is deployed behind HTTPS
  (Phase 6) and point ApiClient.baseUrl at that instead.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
